development:
  adapter: async

test:
  adapter: test

production:
  adapter: redis
  url: url: <%= ENV.fetch("REDISCLOUD_URL") { "redis://localhost:6379/1" } %>
  channel_prefix: first_step_production
